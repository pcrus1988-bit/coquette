# Magento Migration Workspace

This directory contains migration design and non-sensitive migration tooling/documentation only.

Do not commit raw Magento database dumps, order/customer exports, credentials, media archives containing private data, or production configuration.

Private source exports must be stored in dedicated COQUETTE-controlled private storage outside GitHub and referenced by inventory/checksum only when needed.
